/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.scdn.co', 'is1-ssl.mzstatic.com', 'mosaic.scdn.co'],
  },
}

module.exports = nextConfig

